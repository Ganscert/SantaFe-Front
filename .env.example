# Cliente (expuestas al navegador via Vite)
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=us2

# Servidor (Vercel Functions — NUNCA exponer al cliente)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=us2
